stages:
  - test

Test:
  stage: test
  image: python:3.6.4-jessie
  script:
    - pip install -r requirements.txt -r requirements-test.txt
    - python3 manage.py test kip_api
    - coverage run manage.py test kip_api
    - codecov --token=$CI_CODECOV_IO_TOKEN
  only:
    - branches
